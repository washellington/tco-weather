import { AssetImagePipe } from './asset-image.pipe';

describe('AssetImagePipe', () => {
  it('create an instance', () => {
    const pipe = new AssetImagePipe();
    expect(pipe).toBeTruthy();
  });
});
